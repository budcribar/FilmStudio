# GUI + Stage 1 book extract (install into project venv)
#   .venv/bin/pip install -r requirements-review.txt   # WSL/Linux
#   .\.venv-win\Scripts\pip install -r requirements-review.txt  # Windows venv

streamlit>=1.32
pillow>=10.0
pypdf>=4.0
pymupdf>=1.24
# Optional but useful for renderer when system ffmpeg is missing:
# imageio-ffmpeg>=0.5
